export const RequestInvite:React.FC = () => {
    return(
        <div className="request-invite" onClick={()=>console.log('invite sent or something')}>
            Request Invite
        </div>
    )
}